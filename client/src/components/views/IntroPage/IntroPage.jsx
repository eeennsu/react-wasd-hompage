import React from 'react';

const Intro = () => {
    return (
		// 예시 스타일
        <section style={{ display: 'flex', width: '85%', background: 'yellowgreen', margin: '2rem auto 0', height: '80vh' }}>
          IntroPage
        </section>
    );
};

export default Intro;